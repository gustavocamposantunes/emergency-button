# Emergency Button

Basic Flutter app scaffold with a small unit-testable counter example.

## Structure

- `lib/` contains the app entry point, UI, and business logic.
- `test/` contains unit tests for the counter logic.

## Next steps

Run `source ~/.bashrc` once so the Flutter SDK in `$HOME/flutter` is on your PATH, then use `flutter pub get` and `flutter test`.